all:
  vars:
    ansible_user: pi
    # Don't store password here - use --ask-pass flag instead
    # Each Pi ships with a different factory password
  hosts:
    raptor-01:
      ansible_host: raptor3  # Change to IP or hostname
      raptor_id: "01"
    # Add more Pis here:
    # raptor-02:
    #   ansible_host: 192.168.1.102
    #   raptor_id: "02"
    # raptor-03:
    #   ansible_host: 192.168.1.103
    #   raptor_id: "03"
